package com.wwind.notch;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ApplicationSmokeTest {

    @Test
    void contextLoads() {
        // Wiring (law, solver, sequence, validation, web, example runner) is valid.
    }
}
